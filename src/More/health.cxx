//namespace Health{}

/*
class Scene{};
class Entity{};
class Event{};
class Health{
public:
	
	Health(void* parent, int maxHP);
	~Health();
	virtual void update(float delta);
	virtual void render(void* target);
	virtual void handleEvent(Event e);
	
	virtual void Added(Scene s);
	virtual void Removed(Scene s);
	virtual void SceneEnd(Scene s);
	
	void takeDamage(int &amount, const HealthType &type, const float &multiplier);
	void healDamage(int &amount, const HealthType &type, const float &multiplier);
	
	
private:
	int HP;
	int redDmg;
	int shadowDmg;
	int maxHp;
	
	Entity* parent;
	float mHealingStartTime;
	float mHealingStartTimer;
};

enum HealthType{
	HealthTypeNormal,
	HealthTypeShadow
};

*/
